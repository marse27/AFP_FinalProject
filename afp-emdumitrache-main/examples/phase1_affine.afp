// Phase 1: a Light value is affine — passed into a function exactly once.
// The function consumes the Light parameter; the caller cannot use it again.
// Expected output: 1
fn is_red(s: Light) -> int {
    if s == Red then 1 else 0
};
let signal = Red;
is_red(signal)
