// Phase 1: affine types, a Light value can be used at most once.
// is_red consumes the argument, the caller cannot use signal again after this call.
// Expected output: 1
fn is_red(s: Light) -> int {
    if s == Red then 1 else 0
};
let signal = Red;
is_red(signal)