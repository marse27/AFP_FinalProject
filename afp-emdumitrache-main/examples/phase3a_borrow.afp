// Phase 3A: immutable borrows.
// &x creates an immutable borrow, *r dereferences it without consuming r.
// The borrow prevents x from being moved while r is alive.
// Expected output: 42

fn double_via_ref(r: &int) -> int { *r + *r };
let x = 21;
let r = &x;
double_via_ref(r)
